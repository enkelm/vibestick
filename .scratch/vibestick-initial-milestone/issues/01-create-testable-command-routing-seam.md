# 01: Create a testable command-routing seam

**What to build:** Refactor the prototype into independently testable input, configuration, app-context, routing, output, and presentation boundaries without intentionally changing current behavior. Replace embedded self-checks with an XCTest target so later slices can add deterministic evidence while the source-built app remains usable.

**Blocked by:** None (can start immediately).

**Category:** enhancement

**Status:** wontfix

- [x] Input, configuration, app-context, routing, output, and presentation responsibilities have explicit boundaries that can be exercised without launching the full app.
- [x] Existing Xbox report decoding, profile resolution, persistence, and radial-selection self-checks run as XCTest cases.
- [x] The app builds and its existing source-built workflows remain available after the refactor.

## Comments

> *This was generated by AI during triage.*

Closed as already implemented by commit `b05cfd0` (`refactor: extract
testable core`). The package now has separate core, executable, and XCTest
targets; command routing and platform output meet at explicit interfaces; and
the former embedded checks run under `swift test`. Triage verification passed
all 16 tests and built the source application successfully.
