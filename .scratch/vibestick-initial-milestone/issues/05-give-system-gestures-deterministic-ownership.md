# 05: Give system gestures deterministic ownership

**What to build:** Route every controller input to exactly one owner while allowing the visible bindings overlay to observe pass-through input. Make short L3 invoke dictation, long L3 open the app wheel without also invoking dictation, and Share toggle the bindings overlay in every app context.

**Blocked by:** 04: Resolve app contexts and safe ordinary bindings.

**Category:** enhancement

**Status:** wontfix

- [x] Input ownership follows active capture, app wheel, pending system gesture, Herdr layer, then ordinary app binding.
- [x] A short L3 emits the configured TypeWhisper keyboard shortcut exactly once.
- [x] A long L3 opens the app wheel and does not emit the short-L3 action.
- [x] Releasing L3 never activates an app.
- [x] Share toggles the bindings overlay independently of the active app context.
- [x] System bindings can be overridden or disabled without changing app profiles.
- [x] Automated checks cover ownership precedence and short-versus-long L3 timing.

## Comments

> *This was generated by AI during triage.*

Closed as already implemented by commits `49a4e69`, `2233084`, and
`a762f0a`. The command router assigns every normalized input to one owner,
resolves configurable Share and short/long L3 system gestures ahead of app
bindings, preserves overlay pass-through, and prevents a long L3 from also
emitting the short action. Deterministic tests cover ownership precedence,
system-binding overrides and disabling, trigger ownership, and the 650 ms L3
boundary. Triage verification passed all 32 tests.
