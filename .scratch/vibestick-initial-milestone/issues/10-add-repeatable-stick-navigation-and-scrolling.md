# 10: Add repeatable stick navigation and scrolling

**What to build:** Add repeatable left-stick arrow navigation and right-stick scrolling to the supported app contexts using centralized initial tuning and the current Mac's natural-scrolling direction.

**Blocked by:** 08: Deliver the Slack command surface; 09: Deliver the Herdr command surface and Herdr layer.

**Status:** wontfix

- [x] Left-stick directions repeat the configured arrow-key actions after a 400 ms delay at an 80 ms interval outside a 0.25 dead zone.
- [x] Right-stick movement produces repeatable scrolling that follows the current natural-scrolling setting.
- [x] Herdr, ordinary Ghostty, and Slack receive the stick behavior specified for their app contexts.
- [x] Repeat and scrolling stop on release, pause, disconnect, app-context transition, or entry into a higher-priority mode.
- [x] Initial tuning values have one source of truth and persist through the versioned configuration.
- [x] Automated checks cover dead-zone behavior, timing, direction, and cancellation.

## Comments

> *This was generated by AI during implementation.*

Closed as implemented by commit `9c3166c` (`feat: add repeatable stick
input`). Left-stick arrow navigation and right-stick scrolling now fire
immediately and repeat with the centralized 0.25 dead zone, 400 ms delay, and
80 ms interval. Scroll deltas follow the current macOS natural-scrolling
setting. Lifecycle cleanup and higher-priority ownership cancel active repeat
state while the merely visible bindings overlay remains pass-through. Schema 3
persists stick tuning and migrates schema 1 and 2 configurations. Automated
checks cover timing, direction, dead-zone boundaries, release and ownership
cancellation, supported app contexts, natural scrolling, and persistence.
