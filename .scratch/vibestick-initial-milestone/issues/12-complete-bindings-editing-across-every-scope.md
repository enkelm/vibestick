# 12: Complete bindings editing across every scope

**What to build:** Let the operator edit system bindings, global fallbacks, the pinned focused-app profile, the focused Herdr context's Herdr layer, and stick and scroll behavior with keyboard and pointer input while mapped controller output is safely suspended.

**Blocked by:** 03: Migrate to the versioned sparse configuration; 09: Deliver the Herdr command surface and Herdr layer; 10: Add repeatable stick navigation and scrolling; 11: Make the bindings overlay a live pass-through trainer.

**Status:** wontfix

- [x] The editor exposes every configurable scope in the milestone.
- [x] Each binding identifies whether it comes from a preset, global fallback, or operator override.
- [x] The operator can clear or override a binding and reset one binding or an entire app profile.
- [x] Resetting a sparse override reveals the current preset or global fallback.
- [x] Starting editing or key capture pins the edited app context and suspends mapped controller output.
- [x] Live controller visualization continues during editing and pass-through resumes after editing ends.
- [x] Saved edits survive relaunch through the versioned configuration.

## Comments

> *This was generated by AI during implementation.*

Closed as implemented by commit `5571e7f` (`feat: complete bindings
editing`). The overlay now separates preview from active editing, pins the
focused app only for an editing session, and exposes system, global fallback,
focused-app, Herdr-layer, and stick-mapping editor sections. Every entry shows
its provenance and supports keyboard capture, clearing, and single-binding
reset; app profiles also support whole-profile reset. Editing suspends mapped
output without stopping live visualization, clears transient controller state
at both editing boundaries, and resumes pass-through afterward. Automated
checks cover every editor section, sparse reset behavior, persistence, and
editing lifecycle safety.
