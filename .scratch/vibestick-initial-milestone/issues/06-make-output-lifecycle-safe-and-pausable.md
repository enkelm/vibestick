# 06: Make output lifecycle-safe and pausable

**What to build:** Start mapped output automatically while keeping it safe across missing Accessibility permission, emergency pause, controller disconnect and reconnect, app-context changes, and application shutdown. Preserve system-level recovery surfaces whenever ordinary output is unavailable.

**Blocked by:** 05: Give system gestures deterministic ownership.

**Category:** enhancement

**Status:** wontfix

- [x] Output is active on launch and pause resets to active on the next launch.
- [x] First run requests Accessibility permission, while denied permission leaves the app wheel, bindings overlay, and status UI usable.
- [x] Emergency pause disables app shortcuts, Herdr-layer actions, dictation, repeats, and scrolling while preserving Share, the app wheel, menu controls, and live visualization.
- [x] Disconnect and app-context transitions release active output, clear held controls, stop continuous output, and cancel transient modes.
- [x] Reconnect resumes active operation without another menu action.
- [x] The menu bar reports controller, Accessibility, output, and app-context status.
- [x] Automated checks cover pause, disconnect, reconnect, and app-context transition state.

## Comments

> *This was generated by AI during triage.*

Closed as implemented by commit `68e893f` (`feat: make output lifecycle safe
and pausable`). Output now starts active for every launch, requests
Accessibility on first run, preserves system recovery surfaces while ordinary
output is unavailable, and cleans up held and transient state across pause,
disconnect, app-context transitions, and shutdown. The menu reports all four
lifecycle dimensions, and automated checks cover pause, permission loss,
disconnect, reconnect, context transitions, held-control cleanup, recovery
routes, and shutdown.
