# 09: Deliver the Herdr command surface and Herdr layer

**What to build:** Provide the complete Herdr base preset and alternate Herdr layer on positively identified Herdr surfaces. Support both holding Back with another control and tapping Back to arm the next control for two seconds, without emitting partial shortcuts.

**Blocked by:** 06: Make output lifecycle-safe and pausable.

**Category:** enhancement

**Status:** wontfix

- [x] Every Herdr base input emits the literal shortcut specified for the initial milestone.
- [x] Holding Back while pressing another configured control executes the corresponding Herdr-layer action.
- [x] Tapping Back arms the Herdr layer for two seconds; a valid second input executes once.
- [x] Tapping Back again cancels an armed layer, and timeout emits nothing.
- [x] Vibestick emits the complete configured shortcut only after a valid second input.
- [x] Ordinary Ghostty never receives Herdr base or layer commands.
- [x] Automated checks cover hold, arm, cancellation, timeout, and context isolation.

## Comments

> *This was generated by AI during triage.*

Closed as implemented by commit `36d9f43` (`feat: deliver Herdr command
surface`). Vibestick now provides the complete literal Herdr base and Back-layer
presets only on positively identified Herdr surfaces. Back supports held chords,
a two-second armed action, second-tap cancellation, silent expiry, operator
overrides, and lifecycle cleanup without emitting partial shortcuts. Ordinary
Ghostty remains isolated. Automated checks cover every configured shortcut,
hold and arm behavior, cancellation, timeout, unavailable output, ownership
preemption, cleanup, overrides, and context isolation.
